version: 2

models:
  - name: int_lcom_sfdc_account_mapping_base
    description: >
      One validated direct or fallback Salesforce account mapping per LCOM organization.
      It is distributed by Salesforce ID for duplicate-rule processing in lcom_sfdc_account_mapping.
    config:
      tags:
        - common
        - staging
    columns:
      - name: organization_id
      - name: organization_type
      - name: salesforce_id
