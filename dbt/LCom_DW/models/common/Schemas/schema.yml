---
version: 2
models:
  - name: dim_student
    description: Conformed Student dimension base on
      content_delivery_usage.dbo.mv_student_account.
    config:
      tags:
        - dim
  - name: dim_teacher
    description: Conformed Teacher dimension base on
      content_delivery_usage.dbo.mv_teacher_account.
    config:
      tags:
        - dim
  - name: dim_user
    description: Conformed Users dimension base on
      content_delivery_usage.dbo.user_account. It should be a table with info
      from all systems
    config:
      tags:
        - dim
  - name: dim_calendar
    description: Calendar dates and derived attributes - as school Years start and end dates, fiscal years etc. The table was created once and no need to re-run the model. The model is needed only to be consistent with dbt environment
    config:
      tags:
        - dim        