---
sources:
  - name: common
    description: "Common source tables used across multiple models built once per project and static" 
    quoting:
      database: false
      schema: false
      identifier: false
    tables:
      - name: dim_calendar     

  - name: staging
    description: "Some static source data" 
    quoting:
      database: false
      schema: false
      identifier: false
    tables:
      - name: dual
        description: "Single row table to use in queries without from for empty dbt runs"       

  - name: audit
    description: "Audit and logs tables for tracking dbt runs and changes"
    quoting:
      database: false
      schema: false
      identifier: false
    tables:
      - name: dbt_run_log      

  - name: profiles
    description: "SFDC Profiles"
    database: rawdata
    quoting:
      database: false
      schema: false
      identifier: false
    tables:
      - name: vw_sfdc_schema_audit