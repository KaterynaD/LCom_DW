version: 2

models:
  - name: stg_lcom_sfdc_account_keys
    description: >
      Narrow Salesforce account key projection used to validate organization Salesforce IDs
      and build fallback mappings for lcom_sfdc_account_mapping.
    config:
      tags:
        - common
        - staging
    columns:
      - name: account_id
      - name: lcom_organization_c_id
