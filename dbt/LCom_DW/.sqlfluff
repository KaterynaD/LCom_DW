[sqlfluff]
dialect = redshift
templater = dbt
sql_file_exts = .sql,.sql.js,.dml,.ddl
exclude_rules = LT05

# Only lint what matters
paths = models

[sqlfluff:templater:dbt]
project_dir = .
profiles_dir = C:\Users\abutler\.dbt