"""Prefect orchestration package for this workspace.

This package contains:

- production flow entry points under ``flows``;
- shared runtime utilities under ``utils``;
- automated verification under ``tests``.

The package is intentionally named ``prefect_orchestration`` so it does not
collide with the installed ``prefect`` library used by the flows.
"""
